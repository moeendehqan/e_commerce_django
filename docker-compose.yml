
services:
  web:
    build: .
    container_name: ecom_sepidshop
    command: gunicorn e_commerce_django.wsgi:application --bind 0.0.0.0:5500
    volumes:
      - .:/app
    ports:
      - "5500:5500"
    restart: always
